class Categoria {
  constructor(id, descricao, urlImage, refImage) {
    this.id = id;
    this.descricao = descricao;
    this.urlImage = urlImage;
    this.refImage = refImage;
  }
}

module.exports = Categoria;
